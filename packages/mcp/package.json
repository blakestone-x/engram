{
  "name": "@engram/mcp",
  "version": "0.1.0",
  "description": "Model Context Protocol server for Engram — give any MCP-capable agent a tiered, decaying long-term memory.",
  "type": "module",
  "license": "MIT",
  "author": "Blake Stone",
  "keywords": ["mcp", "model-context-protocol", "ai", "agent", "memory", "rag", "claude", "cursor"],
  "bin": {
    "engram-mcp": "./dist/index.js"
  },
  "files": ["dist"],
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -b",
    "start": "node ./dist/index.js",
    "lint": "tsc -b --noEmit || true"
  },
  "dependencies": {
    "@engram/core": "0.1.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
