{
  "name": "@engram/core",
  "version": "0.3.0",
  "description": "Engram engine: markdown vault IO, BM25 search, Ebbinghaus decay, memory consolidation, and a local HTTP API.",
  "type": "module",
  "license": "MIT",
  "author": "Blake Stone",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc -b --noEmit || true"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "yaml": "^2.6.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.19.20",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
